package service

import (
	"GoBIMS/model"
)

func GetBookByID(ID int) model.Book {
	// get data from mysql
	var book model.Book
	return book
}

func SearchByName(name string) model.Book {
	var book model.Book
	// get data from mysql
	return book
}
